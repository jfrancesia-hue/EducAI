name: Trigger Deploy Hooks

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: false

jobs:
  deploy-web:
    name: Trigger apps/web deploy hook (Vercel)
    runs-on: ubuntu-latest
    environment: production
    timeout-minutes: 10
    env:
      DEPLOY_HOOK_URL: ${{ secrets.VERCEL_WEB_DEPLOY_HOOK_URL }}
    steps:
      - name: Validate hook
        run: |
          if [ -z "$DEPLOY_HOOK_URL" ]; then
            echo "Missing production secret: VERCEL_WEB_DEPLOY_HOOK_URL" >&2
            exit 1
          fi

      - name: Trigger deploy
        run: |
          response=$(curl --fail-with-body --silent --show-error -X POST "$DEPLOY_HOOK_URL")
          echo "$response"

  deploy-gov-dashboard:
    name: Trigger apps/gov-dashboard deploy hook (Vercel)
    runs-on: ubuntu-latest
    environment: production
    timeout-minutes: 10
    env:
      DEPLOY_HOOK_URL: ${{ secrets.VERCEL_GOV_DASHBOARD_DEPLOY_HOOK_URL }}
    steps:
      - name: Validate hook
        run: |
          if [ -z "$DEPLOY_HOOK_URL" ]; then
            echo "Missing production secret: VERCEL_GOV_DASHBOARD_DEPLOY_HOOK_URL" >&2
            exit 1
          fi

      - name: Trigger deploy
        run: |
          response=$(curl --fail-with-body --silent --show-error -X POST "$DEPLOY_HOOK_URL")
          echo "$response"

  deploy-api:
    name: Trigger apps/api deploy hook (Render)
    runs-on: ubuntu-latest
    environment: production
    timeout-minutes: 10
    env:
      DEPLOY_HOOK_URL: ${{ secrets.RENDER_API_DEPLOY_HOOK_URL }}
    steps:
      - name: Validate hook
        run: |
          if [ -z "$DEPLOY_HOOK_URL" ]; then
            echo "Missing production secret: RENDER_API_DEPLOY_HOOK_URL" >&2
            exit 1
          fi

      - name: Trigger deploy
        run: |
          response=$(curl --fail-with-body --silent --show-error -X POST "$DEPLOY_HOOK_URL")
          echo "$response"
