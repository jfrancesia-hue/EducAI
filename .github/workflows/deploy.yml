name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy-web:
    name: Deploy apps/web (Vercel)
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4
      - run: echo "Placeholder — configurar Vercel token + project id en Fase 1"

  deploy-gov-dashboard:
    name: Deploy apps/gov-dashboard (Vercel)
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4
      - run: echo "Placeholder — configurar Vercel token + project id en Fase 4 (B2G)"

  deploy-api:
    name: Deploy apps/api (Render/Railway)
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4
      - run: echo "Placeholder — configurar deploy hook Render/Railway en Fase 1"

  deploy-whatsapp-agent:
    name: Deploy apps/whatsapp-agent (Render/Railway)
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4
      - run: echo "Placeholder — configurar deploy hook + ALLOWED_ORIGIN Twilio en Fase 1"

  deploy-worker:
    name: Deploy apps/worker (Render/Railway)
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4
      - run: echo "Placeholder — configurar deploy hook + REDIS_URL managed en Fase 1"
